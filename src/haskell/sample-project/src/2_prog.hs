simple x = x

calcChange owed given = if given - owed > 0
                        then given - owed
                        else 0    
    where change = given - owed

doublePlusTwo x = dounleX + 2
    where dounleX = x*2